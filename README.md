LTG Greetings — Server

This folder contains the server code for LTG Greetings.

Quick setup to create a GitHub repo and push (PowerShell):

```powershell
# Run from this folder
.\push_to_github.ps1 -DefaultRepoName "LTG-Greetings-server"
```

See `push_to_github.ps1` for details.
